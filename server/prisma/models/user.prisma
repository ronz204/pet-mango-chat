model User {
  id       Int    @id @default(autoincrement())
  name     String @unique
  email    String @unique
  password String

  members     Member[]
  messages    Message[]
  invitations Invitation[]

  createdAt DateTime @default(now())

  @@map("users")
  @@schema("mango")
}
