model Room {
  id   Int    @id @default(autoincrement())
  name String @unique

  members     Member[]
  messages    Message[]
  invitations Invitation[]

  createdAt DateTime @default(now())

  @@map("rooms")
  @@schema("mango")
}
